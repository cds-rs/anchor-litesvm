# Migration Guide

The migration guide now lives in **the book**, so the narrative has one home instead of two.

## Read it

- In the book: [Migrating from Raw LiteSVM](../book/src/appendix/migration.md) (build with `mdbook serve book --open`).

It walks the same ground as before: the step-by-step before/after, the pattern-by-pattern comparison, the complete escrow example (493 lines → 106), and the common issues.

## Why this is a redirect

The migration guide is narrative, so it moved into the book alongside the rest of the prose; keeping a copy here would just be a second source of truth that drifts. For how the docs are layered, see [README.md](README.md).
